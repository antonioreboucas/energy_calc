// Configuração de ambiente do EnergyCalc.
// Troque estes valores na mão se o host/porta do backend ou do frontend
// mudar (dev, IP da rede local, produção) — precisa carregar ANTES de
// js/api.js em toda página (ver a ordem dos <script> no fim de cada .html).
const BASE_URL = "http://localhost/energycalc.com.br";
const API_BASE = "http://localhost:8002";
